<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssic.catering.base.mapper.AdminRoleExMapper" >
  <select id="selectAdminRoleDtoByExample" resultType="com.ssic.catering.base.dto.AdminRoleDto">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Aug 04 14:59:20 CST 2015.
    -->
    select
    <if test="example.distinct" >
      distinct
    </if>
    t1.id as id,
	t1.create_time as createTime,
	t1.role_name as roleName,
	t1.no as no,
	t1.pj_no as pjNo,
	t1.post_no as postNo,
	t1.remark as remark,
	t1.seq as seq,
	t1.pid as pid,
	t1.stat as stat
    
    from 
    	t_ctr_admin_role t1
    left join 
    	t_ctr_admin_role_project t2
    on t1.id = t2.role_id
    	    
    <if test="example != null" >
	    <where >
	      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
	        <if test="criteria.valid" >
	          <trim prefix="(" suffix="" prefixOverrides="and" >
	            <foreach collection="criteria.criteria" item="criterion" >
	              <choose >
	                <when test="criterion.noValue" >
	                  and t1.${criterion.condition}
	                </when>
	                <when test="criterion.singleValue" >
	                  and t1.${criterion.condition} #{criterion.value}
	                </when>
	                <when test="criterion.betweenValue" >
	                  and t1.${criterion.condition} #{criterion.value} and #{criterion.secondValue}
	                </when>
	                <when test="criterion.listValue" >
	                  and t1.${criterion.condition}
	                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
	                    #{listItem}
	                  </foreach>
	                </when>
	              </choose>
	            </foreach>
	          </trim>
	        </if>
	      </foreach>
	      
	      <choose>
		      <when test="projectId != null">
		      	<choose>
		      		<when test="example.oredCriteria">
		      			and t2.proj_Id in (#{projectId}) )
		      		</when>
		      		<otherwise>t2.proj_Id in (#{projectId})</otherwise>
		      	</choose>
		      </when>
		      
		      <otherwise>)</otherwise>
	      </choose>
	    </where>
    </if>
    <if test="example.orderByClause != null" >
      order by t1.${orderByClause}
    </if>
  </select>
   
 <select id="selectMaxSeq" resultType="java.lang.Integer">
 	select max(seq) from t_ctr_admin_role
 </select>
</mapper>